<h1>Listing Roles for <%=@organization.name%></h1>

<table>
  <tr>
    <th>Role</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @roles.each do |role| %>
  <tr>
    <td><%= role.name %></td>
    <td><%= link_to 'Edit', edit_organization_role_path(@organization,role) %></td>
    <td><%= link_to 'Destroy', organization_role_path(@organization,role), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Role', new_organization_role_path(@organization) %>
